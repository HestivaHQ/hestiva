name: Temporary About Founder PR Prep
on:
  push:
    branches:
      - seo/about-founder-trust
permissions:
  contents: write
jobs:
  cleanup:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: seo/about-founder-trust
      - name: Remove temporary workflow
        run: |
          git rm .github/workflows/temp-about-founder-pr.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "chore: remove temporary founder PR prep workflow"
          git push origin HEAD:seo/about-founder-trust